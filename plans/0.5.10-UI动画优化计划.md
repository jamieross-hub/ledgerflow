# 0.5.10 - UI&动画优化计划

> 目标：根据UI&动画优化规则，对LedgerFlow进行P0/P1/P2优先级的动画和交互体验优化，提升产品的稳重感、清晰度和低干扰特性。

## 1. 范围

本轮优化覆盖以下内容：
- P0优先级：核心操作反馈、金额展示规范、列表类动效
- P1优先级：页面切换、Toast提示
- P2优先级：响应式适配、深浅模式切换

---

## 2. P0（必做）

- [x] 【增强】实现核心操作反馈：按钮涟漪效果、按压缩放、危险操作脉冲、成功操作闪光 ✅
  - 已新增 `ui-animations.css` 动画样式库
  - 按钮点击有涟漪效果和0.97倍缩放反馈
  - 危险操作有脉冲动画，成功操作有闪光效果
  - 已通过 `npm run build` 验收

- [x] 【增强】规范金额展示：统一使用设计系统颜色变量，确保暗色模式对比度 ✅
  - 已将硬编码的金额颜色替换为 `--color-income` / `--color-expense`
  - 已优化暗色模式下金额的对比度和文字阴影
  - 已新增金额变更动效（amount-up / amount-down）
  - 已通过 `npm run build` 验收

- [x] 【增强】添加列表类动效：新增/删除淡入淡出、拖拽样式、放置指示器 ✅
  - 已实现列表项进入动画（list-item-enter）
  - 已实现列表项退出动画（list-item-exit）
  - 已实现拖拽时样式和放置区域提示
  - 已创建 `useUIAnimations.ts` React钩子库
  - 已通过 `npm run build` 验收

---

## 3. P1（推荐）

- [x] 【增强】页面切换动画：低干扰的页面进入/退出动效 ✅
  - 已实现页面进入动画（page-in）
  - 已实现页面退出动画（page-out）
  - 使用cubic-bezier(0.22, 1, 0.36, 1)缓动函数，稳重不夸张
  - 已通过 `npm run build` 验收

- [x] 【增强】Toast提示动画：平滑的提示出现/消失动效 ✅
  - 已实现Toast进入动画（toast-in）
  - 已实现Toast退出动画（toast-out）
  - 低干扰设计，不打断用户操作
  - 已通过 `npm run build` 验收

---

## 4. P2（可选）

- [x] 【增强】响应式适配：移动端友好的交互优化 ✅
  - 已优化移动端触摸区域（最小44px）
  - 已加速移动端列表动效
  - 已适配移动端金额显示
  - 已通过 `npm run build` 验收

- [x] 【增强】深浅模式切换：平滑的主题过渡动画 ✅
  - 已实现主题切换时的平滑过渡（0.4s ease）
  - 已优化 `ThemeSwitcher.tsx` 组件，添加过渡类
  - 已尊重用户 `prefers-reduced-motion` 偏好
  - 已通过 `npm run build` 验收

---

## 5. 新增文件

- `src/app/styles/ui-animations.css` - 完整的动画样式库
- `src/shared/hooks/useUIAnimations.ts` - 5个实用React动画钩子
- `docs/UI动画优化总结.md` - 详细的优化文档

## 6. 修改文件

- `src/app/styles/index.css` - 引入动画样式
- `src/app/styles/global.css` - 金额颜色优化
- `src/features/theme-switcher/ThemeSwitcher.tsx` - 主题切换过渡

---

## 7. 设计原则遵循

✅ **稳重清晰定位** - 动效克制，避免夸张
✅ **交互反馈优先** - 所有操作都有即时反馈
✅ **列表类动效** - 流畅的进入退出和拖拽
✅ **金额动效规范** - 统一颜色，平滑变更
✅ **低干扰原则** - 动效不打断用户操作
✅ **响应式适配** - 移动端友好
✅ **辅助功能** - 支持减少动效偏好

---

## 8. 验收标准

1. 所有P0/P1/P2任务已完成
2. 新增样式文件已正确引入
3. React钩子可用且类型安全
4. 至少通过 `npm run build` 验收
5. 尊重用户的减少动效偏好
