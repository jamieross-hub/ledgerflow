# 0.5.4 - UI 溢出与布局优化计划

> 目标：修复当前界面中常见的横向溢出、卡片内容撑破、表单/表格在窄屏下挤压错位等问题，优先处理影响主流程的页面。

## 1. 背景

当前 0.5.3 已完成字体体积优化与 Docker 交付补齐，但前端界面仍存在一些零散的 UI 溢出问题，包括：
- 卡片/表格在窄屏下横向撑出
- 长文本、长按钮、长标签未正确换行
- flex / grid 子项缺少 `min-width: 0` 导致内容挤爆
- 局部面板在移动端的滚动与留白不稳定

本计划聚焦“最小代价收口 UI 溢出问题”，不做大规模视觉重设计。

---

## 2. P0（必做）

- [x] 【Bug】修复关于页面版本号未更新问题 ✅
- [x] 现状盘点：定位当前最明显的 UI 溢出页面与组件 ✅
  - 已定位交易页批量操作区、通用卡片/表单长文本、主要表格/列表容器为当前最明显溢出点
  - 风险类型主要为：flex 子项缺少 `min-width: 0`、长文本不换行、窄屏下按钮组拥挤
- [x] 【Bug】修复交易页批量操作区在窄屏下的挤压/溢出问题 ✅
- [x] 【Bug】修复通用卡片/表单项中长文本不换行导致的横向溢出 ✅
- [x] 【Bug】修复主要表格/列表容器在窄屏下的横向滚动与边距问题 ✅
- [x] 构建验收：`npm run build` ✅
- [x] 手动验收：核心页面窄屏下不出现明显横向溢出 ✅
  - 已结合交易页批量操作区、通用卡片文本、主要表格/列表容器的窄屏样式收口完成。

---

## 3. P1（可选）

- [x] 【增强】补充通用 `min-width: 0 / overflow-wrap / word-break` 防御样式 ✅
- [x] 【增强】补充移动端下按钮组自动换行与间距优化 ✅
- [x] 【增强】记录仍然偏大的页面/组件，作为后续样式拆分技术债 ✅
  - 当前 `src/app/styles/global.css` 约 1.2 万行，已明显超出单文件样式维护舒适区
  - 交易页相关样式前缀（`transaction-*`）约 200+ 行，建议后续按筛选区 / 表格 / 洞察区拆分
  - 仪表盘相关样式前缀（`dashboard-*`）约 250+ 行，建议后续按卡片区 / 趋势区 / 模块编排区拆分
  - 财务页相关样式前缀（`finance-*`）约 150+ 行，建议后续按总览 / 表单 / AI 分析区拆分
  - 助手工作台与全局记忆页样式已形成独立块，后续适合迁出为页面级样式文件，降低全局样式回归风险
- [x] 【增强】建立全局样式分层入口，停止继续向 `src/app/styles/global.css` 直接堆积新业务样式 ✅
- [x] 【增强】完成第一批页面级样式拆分（优先 `transaction-*` / `dashboard-*` / `finance-*`） ✅
- [x] 【增强】补充样式分层约定与迁移说明，约束后续新增样式落点 ✅
- [x] 【增强】降低前端首包体积，优先把主要页面改为路由级懒加载 ✅
- [x] 【增强】根据构建产物继续评估 `manualChunks` / 模块级拆分方案，压缩主 JS 包体积 ✅
- [x] 【增强】将 `pdf-lib / fontkit` 改为按需动态导入，继续压缩 `vendor-pdf` 包体积 ✅
  - 交易页导出 PDF 保持动态加载，避免进入主业务包
  - `vite.config.ts` 已为 `pdf-lib / fontkit` 显式拆分 `vendor-pdf`，避免回落到通用 `vendor`
  - 补充 `fontkit` 默认导出兼容处理，避免不同模块形态下注册失败
  - 构建验收通过：`npm run build`

---

## 4. 验收标准

1. 核心页面在常见窄屏宽度下无明显横向溢出
2. 长文本、长标签、长按钮不会撑破卡片布局
3. 构建通过
4. 仅做必要样式优化，不做大规模 UI 重构
