# 0.5.6 - 健壮度与可维护性审查计划

> 目标：对 LedgerFlow 当前代码进行一轮以健壮性、可维护性、边界处理和后续演进成本为重点的审查，优先覆盖近期高频改动区域与明显高风险文件。

## 1. 范围

本轮优先审查以下内容：
- 最近连续改动的交易页 / 首页 / 构建配置相关代码
- 明显超大文件、职责混杂区域
- 近期已经暴露出构建收尾不稳定、移动端样式适配脆弱等信号的代码段

---

## 2. P0（必做）

- [x] 【审查】梳理近期关键改动文件的健壮性风险（空值、异常、重复提交、提示一致性） ✅
  - `src/pages/transactions/TransactionsPage.tsx`：PDF 导出已补并发保护与字体加载失败提示，但整段导出逻辑仍然过长，包含数据整理、换行排版、下载触发三类职责，后续修改容易引入回归
  - `src/pages/dashboard/DashboardPage.tsx`：移动端空白问题暴露出桌面/移动端样式耦合较深，组件结构与样式约束分散，后续继续加欢迎区内容时仍有再次撑高风险
  - `vite.config.ts`：当前 `manualChunks` 规则清晰，但完全依赖字符串匹配，后续新增重包若没人继续维护，容易重新掉回 `vendor`
- [x] 【审查】识别当前最明显的可维护性问题（超大文件、职责耦合、样式堆积、测试缺口） ✅
  - `src/app/styles/global.css` 11941 行：仍然过大，且与 `pages.css` 的职责边界尚未彻底收口
  - `src/pages/transactions/TransactionsPage.tsx` 3408 行：明显超出单页舒适维护范围，交易列表、批量打印、PDF 导出、筛选、弹窗等职责混杂
  - `src/pages/dashboard/DashboardPage.tsx` 1993 行：首页模块装配、趋势分析、AI 文案、模块配置混在一个文件里，阅读和回归成本高
  - `src/features/transactions/components/TransactionTable.tsx` 1272 行：交互比较集中，虽然已有测试，但批量操作、列拖拽、筛选头仍偏重
  - 测试缺口：目前 PDF 导出补了按钮加载态测试，但真正的导出流程、字体失败分支、移动端首页布局缺少更直接的回归保护
- [x] 【审查】给出可落地的修复建议，区分“应立即处理”和“可后续计划化处理” ✅
  - 应立即处理：
    1. 给交易页 PDF 导出逻辑提取单独工具模块，至少拆出“数据格式化 / PDF 绘制 / 下载触发”三层
    2. 给首页欢迎区增加一个最小 UI 回归测试或截图回归，避免再次出现移动端异常撑高
    3. 对 `vite.config.ts` 的 chunk 策略补一段注释或验收说明，减少后续误删概率
  - 可计划化处理：
    1. 继续拆分 `TransactionsPage.tsx`
    2. 继续拆分 `DashboardPage.tsx`
    3. 将 `global.css` 里的 `dashboard-* / transaction-* / finance-*` 继续迁到页面域样式文件
- [x] 【审查】如发现低风险高收益问题，直接最小修复；否则记录进后续计划 ✅
  - 本轮未直接改业务代码，避免在审查阶段夹带大改
  - 建议下一轮直接从“交易页 PDF 导出逻辑拆分”开始，收益最高、风险也最集中
- [x] 【增强】拆分交易页 PDF 导出逻辑，降低 `TransactionsPage.tsx` 职责耦合 ✅
  - 已将 PDF 导出主逻辑抽离到 `src/features/transactions/model/exportTransactionsPdf.ts`
  - 页面层仅保留并发保护、提示文案和调用入口
  - 已通过 `npx tsc --noEmit` 类型校验
- [x] 【Bug】修复首页分类结构区右侧金额/环比卡片文字错位与挤压问题 ✅
  - 已扩大右侧数值列宽度，避免金额与环比文案被裁切
  - 已补足右侧文案区域稳定宽度与右对齐约束，修复错位问题

---

## 3. 验收标准

1. 至少覆盖近期关键改动区域
2. 输出的问题要具体到文件 / 模块 / 风险类型
3. 建议具备执行性，不停留在泛泛而谈
4. 不做无关大改
